# use commands `npx expo prebuild && npx expo run:android` to launch application in android studio

# use command `cd android` -> `./gradlew assembleRelease`


I will update the readme in due time, currently using it to remember what commands I have to use. Im tired, man
